spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
        highlight_sql: false

  liquibase:
    contexts: test

logging:
  level:
    com.rolliedev.ticketflow: INFO
    org.springframework.test.context.jdbc: DEBUG
